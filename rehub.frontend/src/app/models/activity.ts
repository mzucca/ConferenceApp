export interface Activity {
    id: number
    name: string
    district: string
    date: string
    is_participant: boolean
    listeners_num: number
    max_listeners: number
    speaker: number
    status: string
  }